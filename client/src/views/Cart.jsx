import React from 'react'

import Navbar from '../components/Navbar'

const Cart = ({user}) => {
    return (
        <div>
            <Navbar user={user}/>
        </div>
    )
}

export default Cart